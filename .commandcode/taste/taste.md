- Communicates in Chinese (writes in Simplified Chinese). Confidence: 0.85
- When asking for a fix/tool feature, wants a comprehensive, general-purpose solution to an entire class of problems rather than a narrow patch — e.g., "doctor" must detect and fix *all* config problems and be runnable exactly when uiu is broken ("能够解决所有问题的那种办法"), not just handle the current one-off issue. Confidence: 0.75
- Prefers agent-to-agent delegation architecture — their custom agent should orchestrate external coding agents (Claude Code, Codex, OpenCode, etc.) via CLI invocation, not just act alone. Confidence: 0.80
- Prefers CLI-based subprocess integration over API-based integration for wrapping external tools. Confidence: 0.65
- Develops on Windows (paths use `E:\`, `.cmd` wrappers, GBK console encoding constraints). Confidence: 0.85
: 0.85
- Expects agent to reason about execution environment before running commands — UI automation should run in independent system terminal, not IDE built-in terminal (IDE steals focus and breaks window-management scripts). Prefers thinking ahead over trial-and-error. Confidence: 0.70
- Demands extreme leanness ("精简极致") for the agent project: minimal code, minimal deps, high stability; reuse existing primitives rather than inventing new engines, delete dead code as soon as it's noticed, and prefer stdlib/ctypes/win32/pyautogui over adding third-party libraries (e.g., chose ctypes low-level hooks over installing pynput for global input capture). Confidence: 0.75
- Wants the agent to grow unique, real-world automation capabilities — keyboard/mouse macro record & playback for repetitive work in desktop apps, games, and web pages — rather than commodity agent features. Confidence: 0.85
- Prefers a design-doc + explicit-confirmation workflow for sizeable features: write a spec (docs/superpowers/specs/), ask multi-choice questions on the key tradeoffs, and implement only after the user confirms the choices. Confidence: 0.75
- After implementing GUI/desktop-automation features, expects the agent to perform real end-to-end verification on the actual machine itself — launch a real target app, inject/capture real events, replay real operations, and confirm observable results (file/clipboard readback) — rather than leaving manual testing to the user or trusting unit tests alone ("验证吧" delegates verification back to the agent). Confidence: 0.6
- After implementing GUI/desktop-automation features, expects the agent to run real end-to-end verification on the actual machine itself (launch a real target app, capture/inject real events, confirm observable results) rather than leaving manual testing to the user or trusting unit tests alone ("验证吧" — delegates verification back to the agent). Confidence: 0.6
- Benchmarks their agent (uiu) against other agent frameworks and directs uiu to adopt their proven mechanisms — demonstrated repeatedly: after researching Hermes' skill/suggestion learning loop the user said "让uiu学习下" (frequency-aware suggestions ported in); for a self-diagnose/auto-fix "doctor" feature the user directed "这个在openclaw能够找到" (study OpenClaw's doctor and port its detect→repair→re-scan design). Confidence: 0.85
- Cares about distribution/installation parity with the mainstream agent ecosystem: raised converting uiu to npm because "别的agent都是通过 npm install 安装的" — one-line install matching competitors matters even for a Python project. Goes further: end customers must have zero prerequisites — explicitly rejected "customer needs Python" as unacceptable and wants the runtime auto-installed invisibly (npm shell + embedded CPython) so customers perceive only npm. Confidence: 0.85
- For uiu's self-learning/automation features, insists on human-in-the-loop: the agent should detect repeated usage (macro replays, recurring request topics) and only *suggest* automation (list/accept/dismiss), never auto-create skills, macros, or cron jobs without explicit user confirmation — "只建议不擅自创建，用户确认才落地". Confidence: 0.65
- Expects the self-update flow to cover every install flavor, not just the dev git checkout: `uiu update` must keep working for PyPI/npm installs (no .git → pip upgrade path) and should also be able to bring the auto-installed Python runtime up to date instead of leaving the bundled stack stale ("如果将来uiu update 也能一并给更新"). Confidence: 0.7
